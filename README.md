# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

`
npm install
npm run dev
`

или

`
yarn
yarn dev
`
## Сборка

`
npm run build
`

или

`
yarn build
`
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные приложения

### Интерфейс IProduct
- `id` - Хранит уникальный идентификатор товара в виде строки
- `title` - Хранит имя товара в виде строки
- `description` - Хранит описание товара в виде строки
- `category` - Хранит категорию товара в виде строки
- `price` - Хранит стоимость товара в виде числа или ноля
- `image` - Хранит название изображения для карточки товара в виде строки

Задает структуру данных для объектов товаров и массивов их содержащих

### Интерфейс IBuyer
- `payment` - Хранит выбранный способ оплаты в виде уникального значения
- `address` - Хранит адрес пркупателя в виде строки
- `email` - Хранит почтовый ящик покупателя в виде строки
- `phone` - Хранит номер телефона покупателя в виде строки

Задает структуру данных для объектов заказов

### Интерфейс IApi
- `get<T extends object>(uri: string)` - Выполняет GET запрос по указанному URI и возвращает Promise с данными типа T
- `post<T extends object>(uri: string, data: object, method?: ApiPostMethods)` - Выполняет POST запрос с данными и возвращает Promise с ответом типа T

Определяет контракт для классов, выполняющих HTTP-запросы

### Интерфейс IOrderRequest
- Наследует все поля интерфейса `IBuyer`
- `total` - Хранит общую стоимость заказа в виде числа
- `items` - Хранит массив идентификаторов товаров в виде строк[]

Задает структуру данных для отправки заказа на сервер
### Интерфейс IProductsResponse
- `total` - Хранит общее количество товаров в виде числа
- `items` - Хранит массив товаров типа `IProduct[]`

Задает структуру ответа сервера на запрос списка товаров

### Интерфейс IOrderResponse
- `id` - Хранит уникальный идентификатор созданного заказа в виде строки
- `total` - Хранит итоговую стоимость заказа в виде числа

Задает структуру ответа сервера на создание заказа

### Общее назначение
Интерфейсы отвечают за: 
- Валидацию структуры - гарантируя, что объект имеет нужные поля правильных типов
- Автодополнение - IDE подсказывает доступные свойства и методы
- Читаемость кода - явно показывает, какие данные ожидаются
- Переиспользование - один интерфейс можно применять к множеству объектов

## Модели данных

### Класс MProductСatalog
- `productList` - Хранит список всех товаров в виде массива типа `IProduct[]`
- `targetProduct` - Хранит товар, выбранный для подробного отображения карточки товара типа `IProduct | undefined`
- `events` - Хранит объект брокера событий для генерации событий при изменении данных
- Конструктор: `constructor(events: EventEmitter)` - Принимает объект `EventEmitter`, сохраняет его в поле `events`, инициализирует поля `productList` и `targetProduct`
+ `setProductList` принимает массив товаров типа `IProduct[]` и сохраняет его в поле `productList`, генерирует событие `catalog:changed` с новым списком товаров
+ `getProductList` возвращает массив товаров типа `IProduct[]` из поля `productList`
+ `getProductById` принимает `id` в виде строки и возвращает объект типа `IProduct` или `undefined`
+ `setTargetProduct` принимает объект товара типа `IProduct` и устанавливает его значением поля `targetProduct`, генерирует событие `product:selected` с данными товара
+ `getTargetProduct` проверяет данные в поле `targetProduct` и, если поле не пустое, возвращает объект записанного в него товара типа `IProduct`

Отвечает за:
- Хранение товаров, которые можно купить в приложении

Позволяет:
- Хранить данные каталога товаров на главной странице
- Хранить информацию о выбранном товаре для всплывающего окна с подробным описанием
+ Сохранять данные в каталог
+ Получать данные из каталога
+ Получать товар по его `id`
+ Сохранять информацию о выбранном товаре
+ Получать данные о выбранном товаре
+ Получать и передавать изменение через события

### Класс MBasket
- `basketProductList` - Хранит список товаров в корзине в виде массива типа `IProduct[]`
- `events` - Хранит объект брокера событий для генерации событий при изменении корзины
- Конструктор: `constructor(events: EventEmitter)` - Принимает объект `EventEmitter`, сохраняет его в поле `events`, инициализирует поле `basketProductList`
+ `getBasketProductList` возвращает массив товаров из поля `basketProductList` типа `IProduct[]`
+ `addProduct` принимает объект товара типа `IProduct` и добавляет товар в корзину, генерирует событие `basket:changed` с обновленным списком товаров
+ `delProduct` принимает объект товара типа `IProduct` и удаляет товар из корзины, генерирует событие `basket:changed` с обновленным списком товаров
+ `clearBasket` очищает массив товаров в поле `basketProductList`, возвращает `void`, генерирует событие `basket:changed` с пустым списком товаров
+ `getBasketTotal` вычисляет общую стоимость товаров в корзине и возвращает ее в виде числа
+ `getBasketCounter` вычисляет общее количество товаров в корзине и возвращает его в виде числа
+ `hasProduct` принимает объект товара типа `IProduct` и проверяет его наличие в корзине по `id`, возвращает логический ответ типа `boolean`

Отвечает за:
- Хранение товаров, которые пользователь выбрал для покупки

Позволяет:
- Хранить список и данные товаров в корзине
+ Получать список товаров в корзине
+ Добавлять товар в корзину
+ Удалять товар из корзины
+ Очищать корзину
+ Вычислять общую стоимость товаров в корзине
+ Вести счетчик товаров в корзине
+ Проверять наличие товара в корзине
+ Получать и передавать изменение через события

### Класс MBuyer
- `payment` - Хранит информацию о выбранном методе оплаты заказа в виде одного из уникальных значений типа `"card" | "cash" | ""`
- `address` - Хранит адрес покупателя в виде строки
- `email` - Хранит почтовый адрес покупателя в виде строки
- `phone` - Хранит номер телефона покупателя в виде строки
- `events` - Хранит объект брокера событий для генерации событий при изменении данных покупателя
- Конструктор: `constructor(events: EventEmitter)` - Принимает объект `EventEmitter`, сохраняет его в поле `events`, инициализирует все поля пустыми значениями
+ `setBuyerData` принимает объект данных о заказе типа `Partial<IBuyer>`, не требующего наличия всех полей интерфейса, и, на основе его полей, заполняет данными поля объекта класса, при изменении любого поля генерирует событие `buyer:changed` с полными данными покупателя
+ `getBuyerData` возвращает сформированный из данных полей объект данных покупателя типа `IBuyer`
+ `clearBuyerData` очищает данные покупателя, возвращает `void`, генерирует событие `buyer:changed` с пустыми данными
+ `isValid` проверяет правильность заполнения всех полей и возвращает объект с ошибками для каждого невалидного поля типа `{ [key in keyof IBuyer]?: string }`, eсли поле валидно, оно отсутствует в объекте

Отвечает за:
- Хранение данных покупателя

Позволяет:
- Хранить информацию о выбранном методе оплаты заказа
- Хранить адрес покупателя
- Хранить почтовый адрес покупателя
- Хранить номер телефона покупателя
+ Сохранять данные покупателя
+ Получать данные покупателя
+ Очищать данные покупателя
+ Валидировать данные покупателя 
+ Получать и передавать изменение через события

## Слой коммуникации

### Запросы Api

#### Получить весь список товаров: 
` GET https://larek-api.nomoreparties.co/api/weblarek/product/ `
- При успешном запросе `200 ОК` возвращает объект:
```
{ 
    "total": number,
    "items": productItem[] {
        ...{
            "id": string,
            "description": string,
            "image": string,
            "title": string,
            "category": string,
            "price": number | null
        }
    }  
} 
```

#### Получить один товар: 
` GET https://larek-api.nomoreparties.co/api/weblarek/product/${pruduct.id} `
- При успешном запросе `200 ОК` возвращает объект с указанным в запросе id:
```
{
    "id": string,
    "description": string,
    "image": string,
    "title": string,
    "category": string,
    "price": number | null
}
```
- При ошибке `404 Not Found` возвращает объект с указанием ошибки:
``` 
{
    "error": string
} 
```
#### Отправить информацию о заказе: 
` POST https://larek-api.nomoreparties.co/api/weblarek/order `
- При успешном запросе `200 ОК` возвращает объект:
```
{
    "id": string,
    "total": number
} 
```
- При ошибке `400 Bad Request` возвращает один из объектов с указанием ошибки:
``` 
{
    "error": string
} 
```

### Класс MApi
- `api` - Хранит объект, реализующий интерфейс `IApi`
- Конструктор принимает объект типа `IApi` и сохраняет его в поле `api`
+ Метод `getProductList` выполняет GET запрос на эндпоинт `/product/`, преобразует ответ и возвращает Promise с массивом товаров типа `IProduct[]`
+ Метод `createOrder` выполняет POST запрос на эндпоинт `/order/` с данными заказа и возвращает Promise с ответом сервера типа `IOrderResponse`

Отвечает за:
- Взаимодействие с Api сервера "Веб-ларёк"

Позволяет:
- Получать список товаров с сервера
- Отправлять данные заказа на сервер
- Инкапсулировать логику работы с Api эндпоинтами

### Общее назначение
Слой коммуникации обеспечивает:
- Типизацию всех запросов и ответов Api
- Централизованное управление взаимодействием с сервером
- Изоляцию HTTP-логики от бизнес-логики приложения
- Единую точку для модификации Api-запросов

## Слой представления (View)

### Класс Card
Базовый абстрактный класс для всех типов карточек товара

- `_title: HTMLElement` - Хранит элемент заголовка товара
- `_price: HTMLElement` - Хранит элемент цены товара
- `_category?: HTMLElement` - Хранит элемент категории товара
- `_image?: HTMLImageElement` - Хранит элемент изображения товара
- `_button?: HTMLButtonElement` - Хранит кнопку действия
- `_description?: HTMLElement` - Хранит элемент описания товара
- Конструктор: `constructor(container: HTMLElement)` - Принимает корневой DOM-элемент карточки, находит все необходимые элементы внутри него и сохраняет их в поля класса, возвращает экземпляр класса `Card`
+ `set title(value: string): void` - Устанавливает текст в элемент заголовка
+ `set price(value: number | null): void` - Устанавливает цену в элемент цены, если цена равна `null` устанавливает текст "Бесценно"
+ `set category(value: string): void` - Устанавливает категорию в элемент категории и добавляет соответствующий CSS-класс из объекта `categoryMap`
+ `set image(value: string): void` - Устанавливает изображение товара, формируя полный `URL` с использованием `CDN_URL`
+ `set description(value: string): void` - Устанавливает описание товара в элемент описания
+ `set buttonText(value: string): void` - Устанавливает текст кнопки действия
+ `set buttonDisabled(value: boolean): void` - Активирует или деактивирует кнопку действия

Отвечает за:
- Общий функционал для всех типов карточек товара
- Нахождение и хранение DOM-элементов карточки
- Установку данных в соответствующие DOM-элементы

Позволяет:
- Хранить ссылки на все DOM-элементы карточки
- Централизованно управлять отображением данных карточки
- Устанавливать заголовок товара
- Устанавливать цену товара с обработкой нулевой стоимости
- Устанавливать категорию товара с правильным CSS-классом
- Устанавливать изображение товара с правильным URL
- Устанавливать описание товара
- Управлять текстом кнопки действия
- Управлять состоянием кнопки действия

### Класс CardCatalog
Наследует `Card`, карточка товара для каталога на главной странице
- `container: HTMLElement` - Хранит корневой DOM-элемент карточки (унаследовано от `Component`)
- Конструктор: `constructor(container: HTMLElement, actions?: ICardCatalogActions)` - Принимает корневой DOM-элемент и опциональный объект с обработчиками действий, устанавливает обработчик клика на карточку, возвращает экземпляр класса `CardCatalog`

Отвечает за:
- Отображение карточки товара в каталоге на главной странице
- Реакцию на клик пользователя по карточке

Позволяет:
- Отображать товар в упрощенном виде (без описания)
- Генерировать событие при клике на карточку

### Класс CardPreview
Наследует `Card`, карточка товара для модального окна
- `_button?: HTMLButtonElement` - Хранит кнопку действия (унаследовано от Card)
- Конструктор: `constructor(container: HTMLElement, actions?: ICardPreviewActions)` - Принимает корневой DOM-элемент и опциональный объект с обработчиками действий, устанавливает обработчик клика на кнопку действия, возвращает экземпляр класса `CardPreview`

Отвечает за:
- Отображение детальной информации о товаре в модальном окне
- Управление кнопкой добавления/удаления товара из корзины

Позволяет:
- Отображать полную информацию о товаре с описанием
- Генерировать событие при нажатии на кнопку действия
- Изменять текст кнопки в зависимости от наличия товара в корзине

### Класс CardBasket
Наследует `Card`, карточка товара в корзине
- `_index: HTMLElement` - Хранит элемент порядкового номера товара
- `_deleteButton: HTMLButtonElement` - Хранит кнопку удаления товара из корзины
- Конструктор: `constructor(container: HTMLElement, actions?: ICardBasketActions)` - Принимает корневой DOM-элемент и опциональный объект с обработчиками действий, находит и сохраняет элементы порядкового номера и кнопки удаления, устанавливает обработчик на кнопку удаления, возвращает экземпляр класса `CardBasket`
+ `set index(value: number): void` - Устанавливает порядковый номер товара в корзине

Отвечает за:
- Отображение товара в списке корзины
- Предоставление возможности удаления товара из корзины

Позволяет:
- Отображать товар в компактном виде с порядковым номером
- Устанавливать порядковый номер товара
- Генерировать событие при нажатии на кнопку удаления

### Класс Form
Базовый абстрактный класс для всех форм
- `_submitButton: HTMLButtonElement` - Хранит кнопку отправки формы
- `_errors: HTMLElement` - Хранит элемент для отображения ошибок валидации
- `_inputs: HTMLInputElement[]` - Хранит массив полей ввода формы
- Конструктор: `constructor(container: HTMLElement)` - Принимает корневой DOM-элемент формы, находит и сохраняет кнопку отправки, элемент ошибок и все поля ввода, возвращает экземпляр класса `Form`
+ `set errors(value: Record<string, string>): void` - Отображает ошибки валидации в элементе ошибок
+ `set valid(value: boolean): void` - Активирует или деактивирует кнопку отправки формы
+ `clearErrors(): void` - Очищает отображение ошибок
+ `abstract getValue(): Record<string, string>` - Абстрактный метод для получения данных формы

Отвечает за:
- Общий функционал для всех форм приложения
- Управление состоянием кнопки отправки
- Отображение ошибок валидации

Позволяет:
- Хранить ссылки на все элементы управления формой
- Отображать ошибки валидации
- Управлять состоянием кнопки отправки
- Очищать отображение ошибок
- Определять интерфейс для получения данных формы

### Класс OrderForm
Наследует `Form`, форма оформления заказа, шаг первый
- `_paymentButtons: NodeListOf<HTMLButtonElement>` - Хранит кнопки выбора способа оплаты
- `_addressInput: HTMLInputElement` - Хранит поле ввода адреса доставки
- Конструктор: `constructor(events: IEvents, container: HTMLElement)` - Принимает объект событий и корневой DOM-элемент, находит и сохраняет кнопки оплаты и поле адреса, устанавливает обработчики на кнопки оплаты и поле ввода, возвращает экземпляр класса `OrderForm`
+ `set payment(value: string): void` - Устанавливает выбранный способ оплаты, добавляя класс `button_alt-active` соответствующей кнопке
+ `set address(value: string): void` - Устанавливает значение в поле адреса
+ `getValue(): { payment: string, address: string }` - Возвращает данные формы: выбранный способ оплаты и введенный адрес

Отвечает за:
- Отображение формы выбора способа оплаты и адреса доставки
- Валидацию данных первого шага оформления заказа
- Уведомление о действиях пользователя

Позволяет:
- Хранить элементы управления формой заказа
- Устанавливать выбранный способ оплаты
- Устанавливать адрес доставки
- Получать данные формы заказа
- Генерировать события при изменении способа оплаты
- Генерировать события при изменении адреса
- Генерировать событие при отправке формы

### Класс ContactsForm
Наследует `Form`, форма контактов покупателя, шаг второй
- `_emailInput: HTMLInputElement` - Хранит поле ввода email
- `_phoneInput: HTMLInputElement` - Хранит поле ввода телефона
- Конструктор: `constructor(events: IEvents, container: HTMLElement)` - Аринимает объект событий и корневой DOM-элемент, находит и сохраняет поля email и телефона, устанавливает обработчики изменения полей, возвращает экземпляр класса `ContactsForm`
+ `set email(value: string): void` - Устанавливает значение в поле email
+ `set phone(value: string): void` - Устанавливает значение в поле телефона
+ `getValue(): { email: string, phone: string }` - Возвращает данные формы: email и телефон

Отвечает за:
- Отображение формы ввода контактных данных покупателя
- Валидацию данных второго шага оформления заказа
- Уведомление о действиях пользователя

Позволяет:
- Хранить элементы управления формой контактов
- Устанавливать email покупателя
- Устанавливать телефон покупателя
- Получать данные формы контактов
- Генерировать события при изменении полей формы
- Генерировать событие при отправке формы

### Класс Header
Наследует `Component`, компонент шапки сайта
- `counterElement: HTMLElement` - Хранит элемент счетчика количества товаров в корзине
- `basketButton: HTMLButtonElement` - хранит кнопку открытия корзины
- Конструктор: `constructor(events: IEvents, container: HTMLElement)` - Принимает объект событий и корневой DOM-элемент шапки, находит и сохраняет элемент счетчика и кнопку корзины, устанавливает обработчик клика на кнопку корзины, возвращает экземпляр класса `Header`
+ `set counter(value: number): void` - Устанавливает значение счетчика корзины

Отвечает за:
- Отображение шапки сайта с логотипом и иконкой корзины
- Отображение счетчика товаров в корзине
- Предоставление доступа к корзине

Позволяет:
- Хранить элементы управления шапки сайта
- Устанавливать значение счетчика корзины
- Генерировать событие при нажатии на иконку корзины

### Класс Gallery
Наследует `Component`, компонент галереи товаров
- `catalogElement: HTMLElement` - Хранит контейнер для карточек товаров
- Конструктор: `constructor(container: HTMLElement)` - Принимает корневой DOM-элемент галереи, сохраняет контейнер для карточек, возвращает экземпляр класса `Gallery`
+ `set catalog(items: HTMLElement[]): void` - Отображает переданный массив карточек товаров в контейнере галереи

Отвечает за:
- Отображение каталога товаров на главной странице
- Управление расположением карточек товаров

Позволяет:
- Хранить контейнер для карточек товаров
- Отображать массив карточек товаров в галерее

### Класс Basket
Наследует `Component`, компонент корзины товаров
- `_list: HTMLElement` - Хранит список товаров в корзине
- `_total: HTMLElement` - Хранит элемент общей стоимости товаров
- `_button: HTMLButtonElement` - Хранит кнопку оформления заказа
- Конструктор: `constructor(events: IEvents, container: HTMLElement)` - Вринимает объект событий и корневой DOM-элемент корзины, находит и сохраняет список товаров, элемент общей стоимости и кнопку оформления, устанавливает обработчик на кнопку оформления, возвращает экземпляр класса `Basket`
+ `set items(items: HTMLElement[]): void` - Отображает переданный массив карточек товаров в списке корзины
+ `set total(value: number): void` - Устанавливает общую стоимость товаров
+ `set selected(value: boolean): void` - Активирует или деактивирует кнопку оформления заказа

Отвечает за:
- Отображение содержимого корзины
- Отображение общей стоимости товаров
- Управление состоянием кнопки оформления заказа

Позволяет:
- Хранить элементы управления корзиной
- Отображать список товаров в корзине
- Устанавливать общую стоимость товаров
- Управлять состоянием кнопки оформления заказа
- Генерировать событие при нажатии на кнопку оформления

### Класс Modal
Наследует `Component`, компонент модального окна.
- `_closeButton: HTMLButtonElement` - Хранит кнопку закрытия модального окна
- `_content: HTMLElement` - Хранит контейнер для содержимого модального окна
- Конструктор: `constructor(events: IEvents, container: HTMLElement)` - Принимает объект событий и корневой DOM-элемент модального окна, находит и сохраняет кнопку закрытия и контейнер контента, устанавливает обработчики закрытия по клику на крестик, вне модалки и по клавише Escape, возвращает экземпляр класса `Modal`
+ `open(): void` - Открывает модальное окно, добавляя класс `modal_active` и блокируя скролл страницы
+ `close(): void` - Закрывает модальное окно, удаляя класс `modal_active` и разблокируя скролл страницы
+ `set content(value: HTMLElement): void` - Устанавливает содержимое в контейнер модального окна

Отвечает за:
- Управление отображением модального окна
- Обработку закрытия модального окна разными способами
- Блокировку скролла страницы при открытом модальном окне

Позволяет:
- Хранить элементы управления модальным окном
- Открывать модальное окно
- Закрывать модальное окно
- Устанавливать содержимое модального окна
- Закрываться по клику на крестик
- Закрываться по клику вне модального окна
- Закрываться по нажатию клавиши Escape

### Класс SuccessModal
Наследует `Component`, модальное окно успешного оформления заказа.
- `_closeButton: HTMLButtonElement` - Хранит кнопку закрытия модального окна
- `_description: HTMLElement` - Хранит элемент с описанием успешного оформления
- Конструктор: `constructor(events: IEvents, container: HTMLElement)` - Принимает объект событий и корневой DOM-элемент модального окна, находит и сохраняет кнопку закрытия и элемент описания, устанавливает обработчик на кнопку закрытия, возвращает экземпляр класса `SuccessModal`
+ `set total(value: number): void` - Устанавливает сумму списания в элемент описания

Отвечает за:
- Отображение сообщения об успешном оформлении заказа
- Предоставление возможности закрыть модальное окно успеха

Позволяет:
- Хранить элементы модального окна успеха
- Устанавливать сумму списания в сообщение
- Генерировать событие при закрытии модального окна успеха

## Слой презентера (Presenter)
Презентер реализован в файле `src/main.ts` как набор обработчиков событий и функций-координаторов. Не выделяется в отдельный класс, а представляет собой совокупность функций и обработчиков событий, которые связывают `Model` и `View` слои.

### Основные функции
- `updateCatalog(): void` - Обновляет каталог товаров на главной странице, создает карточки для всех товаров из модели
- `updateBasket(): void` - Обновляет отображение корзины, счетчик в шапке и список товаров в модальном окне корзины
- `isOrderValid(): boolean` - Проверяет валидность данных первого шага оформления заказа (способ оплаты и адрес)
- `isContactsValid(): boolean` - Проверяет валидность данных второго шага оформления заказа (email и телефон)

### Обработчики событий от View
Презентер обрабатывает все пользовательские действия, генерируемые `View` компонентами:
- `card:select` - Обрабатывает выбор карточки товара, создает `CardPreview` и открывает модальное окно
- `card:add` - Обрабатывает добавление товара в корзину, вызывает `MBasket.addProduct()`
- `card:remove` - Обрабатывает удаление товара из корзины, вызывает `MBasket.delProduct()`
- `basket:open` - Обрабатывает открытие корзины, отображает модальное окно с содержимым корзины
- `basket:order` - Обрабатывает начало оформления заказа, отображает первую форму оформления
- `order.payment:change` - Обрабатывает изменение способа оплаты, обновляет валидацию формы
- `order.address:change` - Обрабатывает изменение адреса доставки, обновляет валидацию формы
- `order:submit` - Обрабатывает отправку первой формы, отображает вторую форму с контактами
- `contacts:change` - Обрабатывает изменение полей контактов, обновляет валидацию формы
- `contacts:submit` - Обрабатывает отправку второй формы, создает и отправляет заказ на сервер
- `success:close` - Обрабатывает закрытие модального окна успешного оформления

### Обработчики событий от Model
Презентер реагирует на изменения данных в моделях:
- `catalog:changed` - Обрабатывает изменение каталога товаров, вызывает `updateCatalog()`
- `basket:changed` - Обрабатывает изменение содержимого корзины, вызывает `updateBasket()`
- `buyer:changed` - Обрабатывает изменение данных покупателя (опционально, может использоваться для дополнительной логики)

### Принципы работы презентера
- Только слушает события - Презентер не генерирует события, только обрабатывает их
- Координация потоков - Управляет взаимодействием между `View` и `Model` слоями
- Делегирование логики - Вся бизнес-логика делегируется моделям, презентер только вызывает методы
- Обновление `View` - Вызывает методы `View` компонентов только в ответ на события от моделей или при открытии модальных окон

Отвечает за:
- Координацию всего потока данных в приложении
- Обработку всех пользовательских действий
- Реакцию на изменения данных в моделях
- Управление жизненным циклом View компонентов

Позволяет:
- Связывать пользовательские действия (View) с изменениями данных (Model)
- Управлять отображением модальных окон в ответ на действия пользователя
- Обрабатывать валидацию форм и активацию/деактивацию кнопок
- Координировать процесс оформления заказа от начала до конца
- Обрабатывать успешное создание заказа и сброс состояния приложения